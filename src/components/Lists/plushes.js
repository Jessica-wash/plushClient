import React from 'react'

const Plush = props => {
    return(
        <div>
            {/* <span>{props.plush}&emsp;</span>
            <button onClick={(e) => props.}>Done</button> */}
        </div>
    )
}

export default Plush;